import promptSync = require('prompt-sync');

var prompt = promptSync();

const b: string  = prompt("insira valor para b");
console.log(b);
const c: string = "segunda linha";
const d: string = "terceira linha";